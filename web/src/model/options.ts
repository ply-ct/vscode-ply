export interface Options {
    iconBase: string;
    dummyUrl: string;
    runnable?: boolean;
    base: string;
    indent?: number;
    readonly?: false;
    lineNumbers?: boolean;
    folding?: boolean;
    hovers?: boolean;
}
